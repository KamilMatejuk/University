/* Kamil Matejuk */
#include "funs.h"
#include <stdio.h>

/* Napisz funkcję bool palindrom(char napis[]), której argumentem jest łańcuch zna-
ków a wartością true, gdy argument jest palindromem i false w przeciwnym przypadku.

Napisz funkcję int main(void) do przetestowania poprawności działania funkcji palindrom.

Każda z funkcji powinna być w osobnym pliku źródłowym. Przygotuj plik
Makefile do kompilacji i konsolidacji. */

int main()
{
    wczytanie();
    return 0;
}
